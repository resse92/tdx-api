## 1. 连接适配边界

- [x] 1.1 为主市场与 MAC 客户端定义可测试的内部连接、断开和协议调用边界，并保持现有 `tdx.Caller` HTTP 依赖不变
- [x] 1.2 为每个客户端组增加独立且并发安全的主市场、MAC 连接状态和服务关闭状态
- [x] 1.3 增加单元测试，覆盖状态初始化、连接成功、连接失败、重复连接和协议状态隔离

## 2. 启动连接

- [x] 2.1 将 `tdx.New` 改为在统一启动期限内建立全部客户端组的主市场与 MAC 连接并返回初始化错误
- [x] 2.2 实现部分初始化失败时关闭已建立连接和释放全部客户端资源
- [x] 2.3 修改 `cmd/api` 依赖装配，仅在 TDX 初始化成功后启动 HTTP 监听，并记录可诊断的协议类别与错误原因
- [x] 2.4 增加启动测试，覆盖两类连接成功、主市场失败、MAC 失败、部分资源清理和启动错误传播

## 3. 请求前检查与断线恢复

- [x] 3.1 在每次 operation 分派前检查对应协议连接，未连接时在请求上下文和重试上限内同步恢复
- [x] 3.2 网络错误后只清除并断开失败的协议连接，使下一次重试先重连再执行原只读调用
- [x] 3.3 在服务关闭开始后拒绝新的调用和连接恢复，并等待已借出的客户端组归还后关闭连接
- [x] 3.4 增加单元测试，覆盖首次底层主市场调用、首次 MAC 调用、断线重连、重试上限、另一协议连接不受影响及关闭期间不重连

## 4. 就绪状态与部署

- [x] 4.1 修改 `Ready()` 和 `/health/ready`，仅在全部必需的主市场与 MAC 连接可用且服务未关闭时返回成功
- [x] 4.2 增加 HTTP 测试，覆盖启动连接正常、任一协议断开、恢复成功和恢复失败时的就绪响应
- [x] 4.3 更新 README、环境变量和 Compose 运维说明，明确启动连接依赖、健康检查语义及上游不可达时的重启行为

## 5. 集成与最终验证

- [x] 5.1 增加显式启用的真实集成测试，验证进程启动即连接、公告和财务首次调用、MAC 首次调用及连接失败分类
- [x] 5.2 执行 `gofmt`、`go test ./...`、`go test -race ./...` 和 `go vet ./...`，修复全部失败和数据竞争
- [x] 5.3 构建并运行容器，验证连接成功后健康、任一上游不可达时不健康，以及终止期间停止恢复并优雅关闭
- [x] 5.4 执行 `openspec validate ensure-tdx-connection-lifecycle --strict` 并核对主市场、MAC 与容器 delta specs 的场景覆盖

## 6. 连接生命周期日志

- [x] 6.1 为 TDX 服务注入 `slog.Logger`，统一主市场与 MAC 生命周期事件字段，并保持默认构造使用 `slog.Default()`
- [x] 6.2 记录启动连接、请求触发重连、连接成功、连接失败、协议错误断开和服务关闭，避免记录健康请求及业务参数
- [x] 6.3 增加内存日志测试，验证协议类别、客户端组、触发类型、地址和错误原因字段
- [x] 6.4 重新执行单元测试、竞态测试、vet、真实集成测试和 OpenSpec 严格校验
