## 1. 项目基础

- [x] 1.1 初始化 Go 模块，并添加与选定 Go 1.26+ 工具链兼容且固定版本的 Gin、CORS、`gotdx`、参数校验和 OpenAPI 依赖
- [x] 1.2 创建 `cmd/api`、`internal/config`、`internal/httpapi` 和 `internal/tdx` 包结构，并实现可编译的最小服务
- [x] 1.3 实现 HTTP、Gin、CORS、TDX 主机、客户端池大小、超时、重试、请求边界和关闭时间的环境变量默认值及校验
- [x] 1.4 增加配置单元测试，覆盖默认值、有效覆盖值、格式错误和互相冲突的 CORS 配置
- [x] 1.5 为规格包含的每个稳定股票相关方法及明确排除的方法类别创建并记录路由到 `gotdx` 的能力覆盖矩阵

## 2. HTTP 服务基础设施

- [x] 2.1 实现包含 `/api/v1`、标准 JSON 成功和错误响应结构、请求 ID、panic 恢复、结构化日志和统一 404 处理的 Gin 路由器
- [x] 2.2 实现市场、证券代码、日期、业务分页、K 线周期和列表限制的可复用绑定及严格参数白名单
- [x] 2.3 实现统一的上游错误分类，以及参数无效、数据缺失、超时、上游不可用和内部错误到 HTTP 的映射
- [x] 2.4 实现可配置的 CORS 中间件，默认宽松且不允许凭据，并支持来源白名单
- [x] 2.5 增加存活和就绪检查接口，并将就绪状态关联到已校验的上游客户端状态
- [x] 2.6 增加 HTTP 基础设施测试，覆盖响应结构、校验失败、错误映射、CORS 请求与预检、404、存活及就绪检查

## 3. TDX 客户端生命周期

- [x] 3.1 使用配置的主机池、自动选择、超时、并发隔离和清晰的连接所有权实现沪深主市场 `gotdx` 客户端适配器
- [x] 3.2 为主 MAC 和扩展 MAC 主机实现客户端适配器，并应用相同的资源边界和连接生命周期规则
- [x] 3.3 为可安全重试的查询实现有界重连和主机故障转移，不得重试已获得有效响应或已部分读取响应的请求
- [x] 3.4 实现适配器关闭，并集成先排空 HTTP 请求、再关闭全部 TDX 连接的信号处理
- [x] 3.5 使用 fake 增加适配器测试，覆盖串行或池化行为、重试上限、主机故障转移、超时分类、损坏连接替换和关闭
- [x] 3.6 对适配器并发和关闭场景执行竞态测试，并解决全部检测到的数据竞争

## 4. 主市场 API

- [x] 4.1 实现并测试主市场证券数量、按范围或分页查询列表、单个行情、批量行情和详细行情接口
- [x] 4.2 实现并测试股票与指数 K 线、K 线偏移、当前及历史分时或分笔图和图表采样接口
- [x] 4.3 实现并测试当前成交、历史委托和历史成交接口，并执行范围上限及日期校验
- [x] 4.4 实现并测试指数信息、指数动量、筹码分布、集合竞价、热门板块、异动、交易所公告、服务器信息和心跳接口

## 5. 公司资料 API 与内部文件处理

- [x] 5.1 实现并测试财务信息、公司行为和组合 F10 业务接口
- [x] 5.2 移除主市场原始文件元数据、下载和解析公共接口
- [x] 5.3 确认 F10 和板块业务接口不要求调用方提供原始文件名

## 6. 沪深市场边界

- [x] 6.1 实现并测试按证券请求只允许 `.SZ` 和 `.SH` 代码后缀、批量请求由代码推导市场，以及全市场请求只允许 `market=0` 和 `market=1`
- [x] 6.2 从公共路由和上游适配器中移除全部扩展市场能力
- [x] 6.3 实现并测试集合响应中的非沪深市场记录过滤
- [x] 6.4 增加边界测试，证明北京、香港、美国及扩展市场请求不会调用上游

## 7. MAC 板块与股票 API

- [x] 7.1 实现并测试 MAC 板块数量、板块列表、成分列表、成分排序、静态成分行情和排序成分行情接口
- [x] 7.2 移除动态位图、排序和过滤公共参数，板块接口使用服务器默认字段及排序
- [x] 7.3 实现并测试 MAC 批量证券行情、可选日期股票快照、可选日期成交、集合竞价和多日分时接口
- [x] 7.4 实现并测试 MAC 股票摘要、资金流向、服务器信息、K 线偏移、市场监控、证券所属板块和统一证券 K 线接口
- [x] 7.5 移除 MAC 原始文件列表和下载公共接口
- [x] 7.6 将 MAC 板块代码改为必填 `board_symbol` 查询参数，并移除动态板块代码路径

## 8. API 文档与覆盖范围

- [x] 8.1 为每个稳定接口添加注解，并生成提交到仓库的 OpenAPI 请求、响应、参数和错误结构
- [x] 8.2 通过应用提供 OpenAPI 文档和交互式文档路由
- [x] 8.3 增加自动化检查，确保每个已注册稳定路由都有 OpenAPI 操作，且每个包含的 `gotdx` 能力都出现在覆盖矩阵中
- [x] 8.4 增加使用文档，提供沪深主市场、F10 和 MAC 请求的代表性 curl 示例，以及全部环境变量和 API 排除项

## 9. 容器部署

- [x] 9.1 增加 `.dockerignore` 和固定版本的多阶段 Dockerfile，用于构建静态二进制，并以非 root 用户配合证书及生成文档运行
- [x] 9.2 增加 Docker Compose 部署，包含端口发布、环境配置、重启策略、就绪健康检查、信号转发和足够的停止宽限时间
- [x] 9.3 增加环境变量示例文件，并记录 Docker 构建、运行、Compose 启动、健康检查、日志查看和关闭命令
- [x] 9.4 构建镜像并执行容器冒烟测试，验证非 root 运行、启动默认值、配置覆盖、就绪检查、CORS 和优雅终止

## 10. 最终验证

- [x] 10.1 执行格式化、静态分析、单元测试和 `go test -race ./...`，并修复全部失败
- [x] 10.2 对代表性的沪深主市场、F10 和 MAC 上游操作执行显式启用的真实集成测试，并在上游不可访问时清晰跳过
- [x] 10.3 对照覆盖矩阵验证已实现的路由目录，并确认未公开商品、ICFQS 原始、实验性或兼容性方法
- [x] 10.4 严格校验 OpenSpec 变更，并使用最终固定的工具链、依赖版本、接口目录和已知上游限制更新运维文档
