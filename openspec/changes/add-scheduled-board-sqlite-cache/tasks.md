## 1. 配置与数据模型

- [ ] 1.1 在 `internal/config` 增加 SQLite 文件路径和刷新超时配置、默认值及启动校验，并补充环境变量表驱动测试
- [ ] 1.2 定义板块列表、板块成分、加载状态、更新时间和 generation 的领域记录及 `Repository`、`Fetcher` 小型接口，并为 gotdx 响应转换编写测试
- [ ] 1.3 引入纯 Go SQLite 驱动，更新 `.env.example` 和配置说明，确认构建不依赖 CGO

## 2. SQLite 仓储

- [ ] 2.1 实现 SQLite 打开、WAL/busy timeout 设置、幂等 schema 迁移和未完成 generation 清理，并测试新库、重开及不可写路径
- [ ] 2.2 实现板块列表和指定板块成分的有序限量读取、已加载空结果判定及成功更新时间读取，并测试命中、缺失、空结果和重启持久化
- [ ] 2.3 实现单资源原子替换与全量 generation 原子发布，确保失败回滚保留 active generation，并用并发读写和故障注入测试完整性

## 3. 缓存编排与刷新

- [ ] 3.1 实现板块列表和成分的缓存优先读取、缺失时有界回源及成功后写入，并测试缓存命中不调用上游、失败不写空数据
- [ ] 3.2 按资源键合并并发缓存缺失请求，同时保持等待请求的上下文取消语义，并添加并发和竞态测试
- [ ] 3.3 实现板块列表及全部成分的有界并发全量抓取和 generation 发布，测试任一上游或存储失败时保留旧数据
- [ ] 3.4 增加刷新开始、成功、耗时、失败阶段、跳过重叠和更新时间的结构化日志，并验证日志不包含上游原始响应

## 4. 北京时间调度与生命周期

- [ ] 4.1 使用标准库计时器实现 `Asia/Shanghai` 每日 09:00、15:00、20:00 的下一触发时间计算，并覆盖当天各时间边界和跨日测试
- [ ] 4.2 实现不启动预热、禁止重叠刷新、停止新触发及在关闭期限内取消或等待在途刷新，并使用可控时钟测试触发、跳过和关闭
- [ ] 4.3 在 `cmd/api` 按 TDX、SQLite、缓存服务、调度器、HTTP 的顺序装配并逆序关闭，补充各初始化阶段失败清理和刷新期间关闭测试

## 5. HTTP 接入与兼容性

- [ ] 5.1 将 `/api/v1/mac/boards` 和 `/api/v1/mac/boards/members` 接入缓存服务，保持参数校验、`limit`、过滤及统一 JSON 响应不变，并保留 `/api/v1/mac/boards/quotes` 实时上游调用
- [ ] 5.2 使用 fake 仓储和 fake 上游补充 `httptest`，覆盖缓存命中、列表/成分缺失回源、回源错误、并发缺失以及无效参数不触碰缓存或上游
- [ ] 5.3 更新 OpenAPI 描述、README 和能力覆盖文档，确认无新增公共路由、响应结构变更或 CORS 行为变化

## 6. 容器与最终验证

- [ ] 6.1 更新 Dockerfile 和 Compose，为非 root 进程提供可写数据目录、每实例独占的 SQLite 持久化卷及对应环境变量，并验证构建和健康检查
- [ ] 6.2 运行 `gofmt`、`go test ./...`、`go test -race ./...` 和 `go vet ./...`，修复发现的问题
- [ ] 6.3 重新生成并核对 `docs/openapi.json`，运行 `openspec validate add-scheduled-board-sqlite-cache --strict` 并验证 Compose 配置
